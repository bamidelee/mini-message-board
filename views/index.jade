extends layout

block content
  header(class= 'header')
    h1= title
    p Welcome to #{title}

  each message in messages
    div
      h2= message.user
      p= message.text 
      p= message.added 

  a(href= '/new') add message
